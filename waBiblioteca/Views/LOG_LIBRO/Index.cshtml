@model IEnumerable<waBiblioteca.LOG_LIBRO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LIBRO.NOM_LIBRO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.USUARIO.NOM_USUAR)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LIBRO.NOM_LIBRO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.USUARIO.NOM_USUAR)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDD_LOG_LIBRO }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDD_LOG_LIBRO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDD_LOG_LIBRO })
        </td>
    </tr>
}

</table>
